"""Collapse-print validation case (designed-to-fail non-planar cylinder).

Holds the raw experimental data (print photographs under ``print_layers/``,
the measured slicing export ``slicing.json``) and the FEA package ``fea/``
with the CG/DG runners and the paper figure scripts.
"""
